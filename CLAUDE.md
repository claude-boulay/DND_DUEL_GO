# CLAUDE.md - Yu-Gi-Oh! D&D Platform Guidelines

## 1. Project Overview
This project is a full-stack web application designed for running **Yu-Gi-Oh! Tabletop RPG (D&D)** sessions. It integrates official Yu-Gi-Oh! card data (via YGOPRODeck API), custom cards, character sheets with D&D-style stats, real-time dice rolls with luck-based rerolls, shop/booster management, and multi-NPC duel management for the Game Master (GM).

---

## 2. Tech Stack & Docker Architecture

### Stack Specification
- **Frontend:** React (Vite, TypeScript, Tailwind CSS)
- **Backend:** Node.js (Express, TypeScript, Socket.io)
- **Database:** MongoDB (via Mongoose) or PostgreSQL (via Prisma)
- **External API:** [YGOPRODeck API v7](https://db.ygoprodeck.com/api/v7/cardinfo.php)
- **Containerization:** **Docker & Docker Compose (Mandatory)**

### Dockerization Rules
- **Full Containerization:** Every service (Frontend, Backend, Database) MUST be containerized and runnable via `docker-compose up`.
- **Containers Environment:**
  - `frontend`: Node/Nginx alpine container running Vite build / dev server.
  - `backend`: Node alpine container running Express + Socket.io.
  - `database`: Official MongoDB or PostgreSQL image with persistent volume mounts.
- Environment variables must be handled via `.env` files mounted inside Docker containers.

---

## 3. Exhaustive Feature List

### 3.1 Authentication & Room Management
- Account creation and authentication (JWT-based).
- Game Session creation by GM with customizable session settings. Sessions are linked to their creator's account, and to every player who has joined, so both can find and resume them later without re-entering the room code.
- Player join mechanism via unique Room Code (e.g., `YGO-8941`).
- Characters (inventory, collection, decks included) persist with their session and are recovered automatically when a returning player resumes it.
- GM can delete a finished session ; this cascades to its characters and merchants to avoid unbounded data growth. Cards/CardSets are never deleted this way (shared reference data across sessions).

### 3.2 Character Sheet Management (Player & NPC)
- Point-Buy System: 27 base points to allocate across 5 stats (History, Perception, Intelligence, Charisma, Luck).
- Automatic Stat Scaling: +0.5 bonus added to all stats per character level.
- Custom RP Attributes: Character name, backstory, personality, and visual description.
- GM NPC Management: Creation and control of NPC sheets and dedicated NPC decks.
- Player and Npc Inventory : List where you can write what Player or NPC have on them

### 3.3 Real-Time Game Engine & Luck Mechanics
- Server-authoritative D20 (and generic sides) dice roller via WebSockets.
- **Luck Mechanics:** 1 free reroll granted per 2 points above 10 in Effective Luck in duel or in the parties:
  $$\text{Luck Rerolls} = \max\left(0, \left\lfloor \frac{\text{Effective Luck} - 10}{2} \right\rfloor\right)$$
- Anti-cheat validation: Server verifies and decrements Luck pool before broadcasting rerolls.
- Real-time action log broadcasting roll results, rerolls, and player actions.

### 3.4 Cards, Custom Content & Banlist
- Integration with YGOPRODeck API for official cards & booster set mapping.
- **Custom Card Creator:** GM tool to create custom cards, all types (Normal/Effect/Ritual/Fusion/Synchro/Xyz/Link monsters, incl. Pendulum; Spell — Normal/Continuous/Quick-Play/Equip/Field/Ritual; Trap — Normal/Continuous/Counter), with effect text and an uploaded image. A real, GM-authored **Lua effect script is mandatory** at creation (`lua_script`, validated) — this is what lets a custom card run through the real duel engine (see §3.6) exactly like an official card, with no "vanilla"/unscripted fallback. Custom cards are stored in the same `Card` collection as official ones (`is_custom: true`, same `frame_type` vocabulary), so they flow unmodified through deckbuilding, merchants and booster opening. Ownership is per-creator (the GM): a custom card is reusable in **any other session run by that same GM**, not just the session it was created in — not shared across different GMs. A GM can link a custom card to an existing booster/set or spin up a brand-new custom booster on the fly; that booster then behaves exactly like an official one (sellable by a merchant, rarity-weighted opening) except it never shows up in the official YGOPRODeck set browser/import list.
- Custom Banlist System: GM can define session-specific banlists (Banned, Limited to 1, Semi-Limited to 2).

### 3.5 Economy, Shops & Booster Packs
- Custom Currency: GM configures currency name (e.g., Gold, DP). Pricing is per-item, not global : the GM sets the price of each card/booster individually when adding it to a merchant's stock.
- Merchant Creation: GM sets up shops with single cards, booster packs, and custom stock.
- Charisma Haggling: Automated Charisma skill check to calculate price discounts, against a DC/discount configured PER ARTICLE by the GM (not a single merchant-wide DC) — see the roadmap entry for `MerchantShopOverlay.tsx`.
- Booster Pack Opening Simulator: Animated card opening with weighted rarity selection. Add Card open to the collection of the player who open it
- Option for the GM to add one or multiple card to a player (for the start or after a RP event)


### 3.6 Deckbuilding & Duels
- Player Inventory & Collection Manager.
- Deck Builder supporting custom banlist validation. Cards addable to a deck must be in the character's collection, and are automatically split into Main/Extra Deck by card type (max 3 copies/card, 40-60 Main, 0-15 Extra) — except NPC decks built by the GM, which are exempt from the collection requirement.
- Ability to organize duel player against NPC or player versus player (validation of the start of a duel by the GM)
- **Real duel engine:** duels run on the actual EDOPro engine (`ocgcore`, vendored/compiled into the backend, see `engine/ocgcore/`), not a hand-calculated ruleset — see §7 roadmap for what this automates for real (battle math, phase cycle, summon legality, and genuine chain/effect resolution for both official and scripted custom cards).
- **Team duels (Duel Tag), 2 camps × 1-5 participants each, shared LP/field per camp:** explicit user decision — ocgcore hard-codes exactly 2 LP pools (`std::array<player_info,2>`, confirmed by reading `field.h`), so a true individual-LP battle royale (3+ independent camps) is structurally impossible with one ocgcore duel, not just unbuilt; a genuine "1v1v1" was considered and ruled out for that reason. What IS native and now wired up: ocgcore's own Tag Duel mechanism — several "duelists" sharing one camp's LP and field (monster/spell-trap zones are always camp-level, never personal to a participant), with each duelist's own deck/hand/Extra Deck automatically rotating in at the start of that camp's own turns (`OCG_NewCardInfo.duelist`, `field::tag_swap`). See §7 roadmap for the real engine quirk this surfaced and how the app tracks it correctly.

---

## 4. Coding Conventions & Best Practices

### Backend Guidelines (Node.js / Socket.io)
- **Never trust client state:** Validate Luck points, currency, deck validity, and card counts strictly on the server.
- **Socket Events Naming:** Use snake_case event names (`join_game`, `roll_dice`, `reroll_dice`, `open_pack`, `buy_item`).
- **REST APIs:** Use standard REST for authentication, character creation, custom card management, and deck building.
- **External API Caching:** Cache YGOPRODeck API responses locally/in-memory to avoid hitting rate limits (max 20 req/sec). Maybe first download the cards in database and apply a cron to check for new card or update once a week
- Integrations de tests unitaires , d'intégration et end-to-end
### Frontend Guidelines (React / TypeScript)
- Use typed Socket.io client instances.
- Keep state sync lean: broad game state via socket, granular UI state in React components.
- Separate official card components from custom cards using visual tags (`is_custom: true`).

---

## 5. Key Database Models & Schemas

### User & Game Session
```typescript
interface User {
  id: string;
  username: string;
  email: string;
  role: 'admin' | 'user';
}

interface GameSession {
  id: string;
  code: string;
  gm_id: string;
  currency_name: string;
  custom_banlist: string[];
}
```

### Merchant
Stock is mutable after creation : items are added/priced/restocked/removed independently, not fixed at merchant creation time.
```typescript
interface MerchantItem {
  id: string;
  item_type: 'card' | 'booster';
  card_id: string | null;   // set when item_type === 'card'
  set_code: string | null;  // set when item_type === 'booster'
  name: string;              // snapshot for display
  image_url: string | null;
  price: number;             // GM-set, per item
  stock: number | null;      // null = unlimited
}

interface Merchant {
  id: string;
  game_session_id: string;
  name: string;
  description: string;
  items: MerchantItem[];
}
```

### Character Sheet
```typescript
interface CharacterStats {
  history: number;
  perception: number;
  intelligence: number;
  charisma: number;
  luck: number;
}

interface Character {
  id: string;
  user_id: string;
  game_session_id: string;
  name: string;
  is_npc: boolean;
  level: number;
  experience: number;
  money: number;
  backstory: string;
  personality: string;
  visual_description: string;
  stats: CharacterStats;
  remaining_luck_rerolls: number;
  collection: string[];
  decks: { name: string; cards: string[] }[];
}
```

### Custom Card
```typescript
// Réalité de l'implémentation : pas d'interface CustomCard séparée. Une
// carte custom est un document `Card` (voir §5 Card officielle plus haut)
// avec `is_custom: true`, `owner_id` (le MJ créateur — détermine la
// réutilisation inter-parties, pas `created_in_session_id` qui n'est que de
// la traçabilité d'affichage), `ygoprodeck_id` absent (index sparse), et
// `description` qui porte le texte d'effet. `type`/`frame_type`/`race`
// sont dérivés côté serveur à partir d'un formulaire structuré
// (catégorie monstre/magie/piège + sous-type) pour rester dans le même
// vocabulaire que les cartes officielles YGOPRODeck (isExtraDeckFrameType
// fonctionne donc sans distinction custom/officiel). `pendulum_scale` et
// `link_arrows` sont des champs custom-only (null/[] pour les cartes
// officielles). Depuis l'intégration du moteur ocgcore réel (voir §3.6/§7) :
// `engine_code` (number | null, sparse unique) = passcode moteur —
// identique à `ygoprodeck_id` pour une carte officielle, alloué de façon
// synthétique (>= 500 000 000, `Counter.model.ts`) pour une carte custom ;
// `lua_script` (string | null) = script d'effet réel, OBLIGATOIRE et
// validé à la création/mise à jour d'une carte custom (jamais null pour
// `is_custom: true`), c'est ce qui permet à une carte custom de tourner
// dans un vrai duel exactement comme une carte officielle.

---

## 6. Docker Compose Preview (`docker-compose.yml`)

```yaml
version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - MONGO_URI=mongodb://mongo:27017/yugioh_dnd
    depends_on:
      - mongo

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
```

---

## 7. Roadmap & Tasks
- [ ] Create `Dockerfile` for frontend, backend, and write root `docker-compose.yml`.
- [ ] Setup Auth & Session join by room code.
- [ ] Implement YGOPRODeck proxy/cache module on backend.
- [ ] Build Character Sheet creation page (27-point buy validator).
- [ ] Build GM Dashboard (Custom card builder, Shop manager, Banlist config).
- [ ] Build Socket.io Dice & Luck Reroll engine.
- [ ] Implement Booster Pack opening simulator.
- [x] Custom card creator: all monster/spell/trap subtypes, uploaded image, linkable to a new or existing booster, reusable across every session the same GM runs, with a mandatory GM-authored Lua effect script (see §3.4/§5) required at creation.
- [x] **Real duel engine (replaces the earlier hand-calculated duel board entirely — superseded, not layered on top):** duels are driven by the actual EDOPro engine, `ocgcore` (`edo9300/ygopro-core`, C++, AGPLv3+), vendored and statically compiled straight into the backend image (`engine/ocgcore/`, `backend/Dockerfile` `ocgcore-build` stage) and spoken to over a small stdio protocol by `backend/src/services/ocgcoreClient.ts`. Official cards resolve their **real** Project Ignis Lua effect script (bundled `CardScripts`) against **real** stats from Project Ignis' `BabelCDB` (SQLite `cards.cdb`: ATK/DEF/type/attribute/race/level/rank, Pendulum scale, setcodes), keyed by `ygoprodeck_id` doubling as the engine passcode (`engine_code` on `Card`). Custom cards run through the *exact same* engine mechanism via their mandatory `lua_script` (no "vanilla"/unscripted fallback) with a synthetic `engine_code`. What's genuinely automated now (not just tracked): Normal/Special Summon legality and tribute cost, battle calculation, the full phase cycle, and **actual chain/effect resolution** (real LIFO execution of each card's script, not just a structure/legality tracker surfacing effect text for a human to apply) — e.g. Raigeki really destroys its target via its own official script, no hand-written game logic for that card. Per-viewer visibility (superseded/tightened, see the dedicated roadmap entry below — face-down field cards are no longer public): only the GM and a participant's own controller see hand/Extra Deck contents and a face-down field card's real identity; graveyard/banished piles and face-up field cards stay public. `Duel.model.ts` stores only config + a human-readable event log in Mongo — the live game state exists solely in an in-memory per-process child-process registry (`duelEngine.ts`) tied to the running backend process: a backend restart kills any active duel's engine state (`status: 'lost'`, non-resumable — no engine-state serialization API exists, documented limitation, not a bug). Team duels (2 camps × 1-5 participants, shared LP/field per camp) are now reimplemented on this engine — see the dedicated roadmap entry below for how.
- [x] **Duel frontend rebuilt for the ocgcore engine** (`DuelPanel.tsx`, `DuelBoardOverlay.tsx`, `frontend/src/lib/api.ts` — old manual-system shapes like `ApiDuelRules`'s house-rule toggles, `ApiChainLink`, `placeDuelCard`/`attackDuel`/etc. all removed, not layered on). `DuelPanel.tsx`: create/list a 1v1 duel (one character + one deck per camp). `DuelBoardOverlay.tsx`: real-time board — LP/phase/turn header, per-team monster/spell-trap zones + hand (own/GM view only, matching backend secrecy) + graveyard/banished piles, all populated from a new on-demand endpoint (`GET /duels/:id/field`, see below) — and a prompt panel that renders whatever the engine is actually asking for (`pending_prompt`, resolved server-side with real card name/image/stats attached to every option, not bare codes): idle actions (summon/set/activate/phase-change), battle actions (activate/attack), zone placement, target selection, chain response (activate-in-response or pass — not just pass), yes/no. Real-time relay reuses the existing `session_resource_changed` Socket.io convention (new `'duels'` resource, emitted after every state-changing duel route) rather than a bespoke event — matches "keep state sync lean" (CLAUDE.md §4) and required no new socket event, just a refetch-on-notify like characters/merchants already do. Chain-response friction is a per-viewer, localStorage-persisted 3-way toggle (`DuelBoardOverlay.tsx`, purely client-side, no server concept of it): **ON** always shows the chain prompt, even when the only legal response is to pass; **Auto** shows it only when the viewer's controlled participant actually has a card it could activate in response (`options.length > 0`), auto-passing otherwise; **Off** auto-passes everything except an engine-`forced` prompt (a mandatory/timed effect, which per the OCG protocol can never be passed) — a forced prompt with several options still shows the buttons, since the order of activation is still a real choice. Auto-pass only ever fires for a prompt the viewer actually controls (GM, or the character's own owner) — never for the opposing camp's decision.
  - New backend pieces this needed, found and built along the way: `GET /duels/:id/field` (per-zone board query via `OCG_DuelQueryLocation`, reverse-engineered from `ocgapi.cpp`/`card.cpp` exactly like the `MSG_*` formats before it — self-describing TLV entries per card, terminated by a `QUERY_END` marker); decoders for `MSG_SELECT_CHAIN` (which cards can actually be chained, not just "pass") and `MSG_SELECT_YESNO`/`MSG_SELECT_EFFECTYN` (both carry a `playerid`, previously undecoded); a `wrong_participant` permission check (a prompt's `playerid` now must match the acting participant — every handled prompt type carries it at the same byte offset, confirmed by reading `playerop.cpp`) so a player can't answer on the wrong camp's behalf.
  - Two real correctness bugs surfaced (and fixed) by actually driving the full flow end-to-end rather than assuming the lower-level pieces composed correctly: (1) `OCG_DuelProcess` only processes until it has generated *a* message batch, not until it reaches a real prompt — a single `process()` call per action (the original design) silently stalled the duel mid-resolution; fixed with `pumpUntilSettled()` (`duelEngine.ts`), which loops until the engine actually reaches AWAITING/END. (2) the stdin/stdout protocol has no request IDs, so concurrent calls into the same duel's `OcgcoreDuel` (e.g. `/field`'s per-zone queries fired via `Promise.all`) desynchronized responses and hung; fixed by serializing every command through an internal queue inside `OcgcoreDuel` itself, so any caller anywhere is safe by construction.
- [x] **Remaining engine prompt types decoded** (`MSG_SELECT_TRIBUTE`, `MSG_SELECT_POSITION`, `MSG_SELECT_OPTION`) — 10 of the engine's prompt types are now handled end to end (up from 7): a tribute-cost Normal Summon (Level ≥5) now surfaces a real tribute picker (`select_tribute`, same card-selection response encoding as `MSG_SELECT_CARD` under the hood — both funnel through the engine's `parse_response_cards`, confirmed in `playerop.cpp`) instead of dead-ending on "unhandled"; an explicit Attack/Defense (and face-up/face-down) choice surfaces as `select_position` with buttons for whichever position bits the engine actually offers; a multi-variant effect choice surfaces as `select_option` (no decodable text — no structured effect-text data exists, see the custom-card rationale above — just a raw per-option identifier to cross-reference against the card's printed text). All three verified against the real engine before shipping: `MSG_SELECT_TRIBUTE`'s wire format (and its resolution: the released monster actually leaves the field for the graveyard) was driven end-to-end through the real HTTP API with two real BabelCDB monsters (Celtic Guardian tributed to Normal Summon Summoned Skull, `duel.e2e.test.ts`); `select_position`/`select_option` were confirmed against direct reads of `playerop.cpp` (`field::process(SelectPosition)`/`field::process(SelectOption)`) and covered with source-derived unit tests, the same rigor used for every other message format in this system, though not (yet) driven through a live custom-script scenario the way tribute was.
  - **Second real correctness bug surfaced along the way** (found while testing the tribute flow's rejection path, not something anticipated going in): an invalid response to *any* prompt makes the engine emit `MSG_RETRY` — still `AWAITING`, but *without* re-sending the original prompt's data. The app was overwriting `state.pendingPrompt` with that bare `MSG_RETRY` message, a type no route recognizes — one bad submission (e.g. wrong tribute count) permanently soft-locked the duel in `wrong_prompt` for every future request. Fixed in `applyMessages` (`duelEngine.ts`): `MSG_RETRY` no longer overwrites the pending prompt, so the engine's "ask the same question again" is honored instead of orphaning it — covers every prompt type, not just the three new ones.
- [x] **Team duels (Duel Tag), 2 camps × 1-5 participants each, shared LP/field per camp** (`Duel.model.ts`, `duelEngine.ts`, `duel.routes.ts`, `DuelPanel.tsx`, `DuelBoardOverlay.tsx`) — generalizes the old system's `MIN_TEAMS=2/MAX_TEAMS=2/MAX_TEAM_SIZE=5` on the real engine, per explicit user decision (a true individual-LP battle royale was considered and ruled out — see §3.6, ocgcore hard-codes exactly 2 LP pools). Each participant is tagged `team` (0/1) + `duelist_index` (0-based, order added — becomes the engine's `OCG_NewCardInfo.duelist`); the engine automatically rotates which duelist's deck/hand/Extra Deck is "live" for a camp via `field::tag_swap` (`MSG_TAG_SWAP`, decoded), the app just reacts and exposes `is_active` per participant — hand/deck counts are resynced straight from the engine's own report on every swap (ground truth, not an incremental guess).
  - **Real engine quirk found and worked around by construction, not by special-casing:** `tag_swap`'s trigger condition checks the GLOBAL turn counter (`infos.turn_id != 1`), not the camp's own turn count — confirmed by directly reading `processor.cpp` AND reproducing it live (added `duelist` to the `CARD` command in `server.cpp`/`ocgcoreClient.ts` for the experiment). Consequence: the camp that loses the opening coin flip has its rotation fire on its own very first turn, skipping straight to `duelist_index` 1 instead of starting on 0 — real EDOPro/ocgcore behavior, not a bug. The fix was to never predict the active duelist from the turn number at all: the app counts real `MSG_TAG_SWAP` events per camp and advances `activeDuelistIndex[camp] = swapCount % campSize` in direct reaction — verified by driving both cases live (camp going first: 0,1,0,1,...; camp going second: 1,0,1,0,...) and proven mathematically equivalent to the engine's own internal round-robin regardless of who moves first, so no knowledge of the coin-flip outcome is ever needed.
  - `DuelPanel.tsx`'s create-duel form now adds/removes up to 5 participants per camp; `DuelBoardOverlay.tsx` shows the full roster per camp (highlighting whoever is `is_active`) and always acts through the camp's currently-active participant, never a frozen teammate — enforced server-side too (`requireActiveParticipant`, not just "controls *a* participant on this camp").
  - Verified end-to-end against the real engine with a genuine 2-duelist-vs-1 duel over the full HTTP API (`duel.e2e.test.ts`): correct `duelist_index`/`is_active` at creation, the inactive teammate rejected from acting even though legitimately controlled, the real rotation observed via polling (not assumed), and the newly-active duelist correctly gaining control after it.
- [x] **Custom card effect proven end-to-end (creation → deck → duel → real activation → real resolution)** (`customCard.e2e.test.ts`) — closes a real coverage gap: until this test, nothing actually *activated* a custom card's Lua script in a duel and checked the effect resolved (only "loads without error" or "usable as a target" were covered). A single-target-destroy Normal Spell was hand-written (Project Ignis structure: `CATEGORY_DESTROY` + `EFFECT_TYPE_ACTIVATE` + `Duel.SelectTarget`/`Duel.GetFirstTarget`/`Duel.Destroy`), validated live against the real engine via a scratch driver *before* being wired into the HTTP test (established discipline this session) — first draft used `CATEGORY_TOTARGET`, which doesn't exist as a global constant in this engine build and fails silently (`LOG ERROR`, never surfaced by `OcgcoreDuel`'s normal `MSG`-only parsing; only visible by reading the raw text protocol directly); fixed by dropping it (`CATEGORY_DESTROY` alone is enough, matching the real Raigeki script's own pattern). The HTTP test then drives a real duel over the full API: opponent Normal-Summons a real BabelCDB monster (Celtic Guardian), the custom spell is activated once legal, the resulting `select_card` prompt resolves the real target's name (not a bare code), the target is selected, and `/field` confirms it actually left the monster zone for the graveyard. One more real bug caught by driving the *full* flow rather than assuming the pieces composed: after `select-card` resolves, the engine opens *another* chain window (both camps can still respond) before the link actually resolves — skipping it made the `/field` check race ahead of the real resolution; fixed by looping `passOptionalChains` again after target selection, same helper as everywhere else.
- [x] **Duel visibility/authorization tightened: GM cannot act as a player, prompt detail is per-team, face-down field cards stay anonymous** (`duel.routes.ts`) — three real usability/security bugs found in manual GM testing, not from a spec: (1) the GM could submit actions for a player-controlled participant, not just NPCs — a genuine leak of player agency to the GM seat; (2) every viewer received full prompt detail (hand contents, activatable options) for whichever participant the engine was currently prompting, not just that participant's own team; (3) a face-down monster's real name/image leaked through target-selection option lists (e.g. picking an attack target), even though nobody should know what a face-down card is before it's revealed — same leak existed in `/field`'s board view.
  - New `canActForParticipant(participant, userId, session)` (acting — strict) is now separate from `isControllerOfCharacter` (viewing — the GM still sees everything): NPC actions require GM, player actions require the player's own `user_id`, and `loadActionContext` enforces this on every one of the 9 action routes instead of the old "GM or controller" check.
  - New `computeCanSeeTeam(duel, userId, session): [boolean, boolean]` (GM ⇒ `[true,true]`; otherwise per-team via `isControllerOfCharacter`) drives two redactions: `describePendingPrompt` returns `{type, playerid, redacted: true}` instead of full detail when the viewer can't see the prompting team, and a new `redactFaceDown(options, actingTeam, isGm)` nulls the `card` field (name/image/stats) on any face-down, opponent-controlled entry inside `select_card`/chain option lists — applied identically in `/field`'s per-zone `toBoardCard` so board view and prompt view can't disagree with each other. Tribute lists are exempt (a tribute can only ever target the tributing player's own monsters, never hidden).
  - Verified against the real engine, not just asserted: `duel.e2e.test.ts` gained a describe block with two real player accounts on opposing teams (GM can't act for either — 403; the non-active player's `GET` shows `redacted: true` while GM and the active player see full detail); `customCard.e2e.test.ts` gained a live scenario where a monster is set face-down (`MSET`) and then legally targeted by a custom effect that doesn't require face-up (deliberately dropping the `IsFaceup()` filter a normal target effect would have) — the resulting `select_card` option has `card: null` for the *acting, authorized* player, while a simultaneous GM `GET` on the same prompt shows the real card name; the effect still resolves correctly on the right card underneath (confirmed destroyed via `/field`) proving redaction doesn't break the actual game logic, only what's exposed over HTTP.
- [x] **Frontend/backend GM-authorization mismatch fixed (real bug, GM-reported: the "vous ne contrôlez pas ce participant" toast flickering in a loop during the opponent's turn)** (`DuelBoardOverlay.tsx`) — root cause: the frontend's `controlsParticipant` helper still used the OLD, more permissive semantics (GM controls every participant, for viewing) for a decision that actually needs the STRICT acting semantics (`canActForParticipant` server-side, GM only for NPCs) tightened in the entry above. The chain-response auto-pass effect used the permissive check, so a GM spectating a *player's* chain prompt (with their local chain-mode set to `auto`/`off`) auto-fired an action the server correctly 403'd — and because the effect depended on `busy` and nothing prevented a retry, the moment `busy` flipped back to `false` the same doomed call fired again, forever, flashing the error toast on and off (and capable of pegging the tab, plausibly the reported black-screen). Fixed two ways, independently: (1) a new `canAct(participant)` in the frontend now mirrors the backend exactly (`is_npc ? session.is_gm : own character`) and gates the auto-pass effect *and* every interactive control in the redesigned board below; (2) a `lastAutoChainKeyRef` records a content signature (team/forced/options) of the last chain prompt already auto-attempted — a failed attempt is never retried for the same logical prompt, so even an unrelated future mismatch can't loop forever. `PromptPanel` also gained a `readOnly` mode (driven by the same `canAct` check) that replaces every interactive control with a passive "not your decision" message for a viewer who is not authorized to submit it, rather than letting them click into a guaranteed 403.
- [x] **Duel board interaction rework: act from the hand/board directly, targeting by clicking zones, a real card back** (`DuelBoardOverlay.tsx`) — GM-requested usability pass, built on top of the authorization fix above (its `canAct`/`resolveInteraction` gate every new interactive surface). `resolveInteraction(location, sequence, controller, boardCard)` is the single point of truth for what a rendered card (hand slot or field zone, own or opponent's) can do right now, driven purely by matching the current prompt's option arrays on `(location, sequence, controller)` — the exact same triple `ApiPromptCardOption` already carried, previously unused for this purpose. Idle/Battle prompts: a hand or field card lights up with a colored ring — amber for activatable, sky-blue for summonable/settable, rose for a legal attacker, emerald for reposition-only — and clicking it opens a small floating popup (the card enlarged, round icon buttons above it for each legal action) instead of the old scrolling option lists; those lists are now gone from the idle/battle panel entirely (replaced by a one-line hint + the phase-transition buttons, which aren't tied to a specific card). `select_place`/`select_card` prompts: the matching board zones (or hand slots) become directly clickable to toggle the same selection state the existing list-based panel already used — both paths write to identical state, so the panel's list stays as a fully-working fallback (needed for non-field targets like graveyard picks) rather than being replaced. Chain/tribute/position/option/yes-no prompts are unchanged (list-based), a deliberate scope cut for this pass. Card backs: the bare "?" is replaced everywhere (mini cards, action popup, pile modal, `BoosterOpeningOverlay.tsx`'s pending/flip slots) by `CardBack`, rendering the real official Yu-Gi-Oh! card back (`frontend/src/assets/card-back.jpg`, supplied by the user) — superseded an earlier original CSS-drawn placeholder shipped first for IP caution; the user confirmed using the genuine art is fine for this private, non-commercial tool, consistent with how the app already displays official card art everywhere via YGOPRODeck.
- [ ] **Duel board interaction, remaining scope cuts from the click-to-act rework above**: chain response, tribute, position, and option prompts are still list-based only (not click-to-act on the board) — a deliberate scope cut, not an oversight. The action popup's position is a fixed pixel offset from the clicked card's bounding rect, not dynamically sized to the popup's actual content — acceptable for now but not pixel-perfect on all card aspect ratios/screen sizes.
- [x] **Two more GM-reported usability fixes on the click-to-act rework** (`DuelBoardOverlay.tsx`): (1) the chain-response default was `'on'` (always shows the "activate in chain, or pass?" panel, even with zero real options) — since a chain priority window opens after nearly every action in real OCG rules (not a bug), this read as "asking to activate when nothing could be activated"; default flipped to `'auto'` (only asks when the viewer's participant genuinely has something to activate, silently passing otherwise — still user-changeable via the existing 3-way toggle). (2) `select_card`/`select_tribute` option rows only ever toggled selection on click — there was no way to preview a *revealed* option (e.g. picking a card to add from your own deck) before deciding; each row is now a dedicated preview button (the card art) beside a separate toggle-selection button, mirroring the pattern the `chain` list already used.
- [x] **Real regression in the (1) fix above: `'auto'`/`'off'` stopped auto-passing after their very first success** — the loop-guard ref (`lastAutoChainKeyRef`, added to stop a *failed* auto-pass retrying forever) keyed on prompt *content* (participant/team/`forced`/options list). An empty-option chain window — by far the most common case, since a priority window opens after nearly every action — produces the SAME signature every single time, so once the first one auto-passed successfully, every later one (a genuinely different, later event) matched that cached key and got silently skipped: GM-reported as "as if 'auto' and 'off' were always treated like 'on'." Fixed by only ever writing the ref on *failure*, and clearing it on *success* — a successful auto-pass no longer blocks the next one, even with an identical signature; only a real, repeated failure for the exact same still-pending prompt stays blocked.
- [x] **GM omniscience walked back for any duel where the GM is actually piloting an NPC — "MJ voit tout" now only holds for pure supervision** (`duel.routes.ts`) — the investigation above turned out to have the right diagnosis (GM seat) but the wrong verdict: the user confirmed they were controlling the NPC and explicitly said they should still NOT see the opponent's hidden info even then — "ça ruine le RP" (breaks immersion) to have metagame knowledge for a side they're actively playing. This reverses the blanket "GM sees all" rule from the entry above, which was correct for a MJ purely *supervising* a player-vs-player duel but wrong for a MJ *playing* one side of it.
  - `computeCanSeeTeam(duel, userId, session)` no longer short-circuits to `[true,true]` for any GM. It computes per-team visibility purely from character ownership (`isControllerOfCharacter`, now GM-agnostic — an NPC's `user_id` is already its creating GM, so a GM controlling an NPC gets recognized as that team's controller with no special case needed) and only falls back to full `[true,true]` visibility when the GM controls **zero** participants in *this specific duel* (i.e. a genuine player-vs-player duel with no NPC in it at all — pure supervision, unchanged from before). The moment a GM pilots an NPC in a duel, they see exactly what that NPC's controller would see — their own team's hand/face-down cards, never the opponent's — identically to a real player.
  - `redactFaceDown` dropped its `isGm` bypass entirely; it now takes the same `canSeeTeam: [boolean, boolean]` tuple and redacts a face-down card whenever `!canSeeTeam[card.controller]`, for anyone, GM included. Since `describePendingPrompt`'s outer redaction gate already guarantees `canSeeTeam[promptTeam]` is true for whoever legitimately reaches this code, this cleanly generalizes without a separate acting-team parameter.
  - Every existing "GM sees everything" assertion across `duel.e2e.test.ts`/`customCard.e2e.test.ts` that exercised a GM-controlled-NPC duel was flipped to assert the new, correct behavior (GM sees only their own NPC's secrets, gets `redacted: true` for the opponent's live decisions) — the *only* untouched "GM sees everything" case is the dedicated player-vs-player describe block, which has no NPC at all and correctly keeps full GM supervision. Several test helpers that fetched `pending_prompt` detail via a blanket `gm.token` (a pre-existing convenience, not a visibility test) had to start re-fetching via whichever participant's token actually owns that decision — this was the real trigger for the cascading test failures this change surfaced, not a flaw in the new logic itself.
  - Full suite verified green (263/263) after every affected assertion was individually re-audited and updated, not just patched to pass.
- [x] **Duel board layout, per-viewer** (`DuelBoardOverlay.tsx`) — GM-reported usability gaps, now fixed. `myTeam` (0/1/null) is derived from which team the viewer actually controls a *character* in (`characters.find(...).user_id === currentUserId`), never from `session.is_gm` — a GM spectating with no personal character in the duel gets `null` and keeps the plain, unmirrored 2-section layout (their own reasonable default, since the GM has no "own" side to feature). When `myTeam` is set: `teamRenderOrder` puts the opponent's section first (top/farthest) and the viewer's own last (bottom/nearest); the opponent's zone rows are reversed (`mirrored`), the viewer's own never are. Confirmed live against the real engine (temporary instrumented test run, reverted after) that `spell_trap_zones` is always 8 slots (0-4 normal S/T, 5 = Field Zone, 6-7 = Pendulum Zones) and `monster_zones` is 7 (0-4 main + 5-6 Extra Monster Zones) under the `DUEL_MODE_MR5` flags this app uses — the Field Zone slot (index 5) is pulled out of the row and pinned to its left edge (right edge once mirrored), matching the request; the rest of the S/T row and the full Monster row render in order (reversed together when mirrored, so corresponding zones roughly face each other like a real dueling mat). The viewer's own hand is pulled out of its `ParticipantBoard` card entirely and rendered by a new `HandBar`, pinned to the bottom of the board's scroll area — visible only when `myTeam` is set (a spectating GM still sees hands inline in each team's card, as before, since there's no single "own" hand to feature for them).
- [x] **Booster opening: single quick spin for Common/Rare, hover-to-zoom on revealed cards, "next pack"/"another set" continuation** (`BoosterOpeningOverlay.tsx`, `CharacterList.tsx`) — three GM-requested usability passes. (1) `is_rare_reveal` (already computed server-side, Super Rare and above) now also drives the reveal ANIMATION, not just the "hero" zoom-in: Common/Rare settle after one quick spin (`QUICK_SPIN_DURATIONS`) instead of the full 5-step decreasing-duration buildup (`SPIN_DURATIONS`, kept for genuinely rare pulls). (2) `SettledSlot` scales up (`hover:scale-[2.5]`, `z-20`) on mouse-over so the card's printed text is actually legible, relying on the grid's pre-existing `[overflow:visible]`. (3) `onNext`/`otherSets`/`onOpenOther` props let the SAME overlay instance chain straight into the next booster of the current set, or into a *different* sealed set entirely, once a reveal finishes — no more forced trip back to the main character view between packs; the reveal-progress state is now explicitly reset when `cards` changes so reusing the instance replays the animation instead of showing the new pack as if already revealed.
- [x] **Luck reroll audit, prompted by a direct user question ("does it still work, including haggling and duel coin/dice rolls?")** — three-part finding, one real gap fixed:
  - **General d20/RP rolls** (`sockets/index.ts` `roll_dice`/`reroll_dice`, `rollStore.ts`): untouched by any engine work this session, confirmed still fully server-authoritative (atomic `remaining_luck_rerolls` decrement).
  - **Merchant haggling** (`merchant.routes.ts`): was a genuine pre-existing gap, unrelated to ocgcore — the haggle roll was computed and resolved in a single atomic `POST .../purchase` call, with no reroll option at all. Fixed by splitting it: new `POST /:id/items/:itemId/haggle` rolls and returns a `PendingHaggle` (new `utils/haggleStore.ts`, same in-memory/TTL pattern as `rollStore.ts`) without buying; new `POST /:id/haggle/:haggleId/reroll` spends a Luck reroll on it (identical atomic-decrement guard to `reroll_dice`) and re-resolves `total`/`success` from the *same* negotiated modifier/DC; `purchaseSchema` gained an optional `haggle_id` that finalizes a previously-rolled (and possibly rerolled) negotiation, consuming it so it can't be replayed — the original one-shot inline `haggle: {modifier}` stays supported for a no-reroll-desired purchase (superseded shape, see the entry below — `discount_percent` was later dropped from this body too once discount became per-article). `PurchaseWidget` now rolls first ("Lancer le marchandage"), shows the result, offers "Relancer (Chance : N)" while `remaining_luck_rerolls > 0` and the roll failed, then "Acheter" to finalize.
- [x] **Merchant shop redesigned as a dedicated, stylized overlay; haggle DC/discount moved from a single merchant-wide value to per-article, GM-editable in that same view** (`MerchantShopOverlay.tsx` — new, `MerchantPanel.tsx` — gutted to a merchant picker, `Merchant.model.ts`, `merchant.routes.ts`) — GM-requested redesign, two parts. (1) `MerchantItemAttrs` gains `haggle_dc: number | null` / `haggle_discount_percent: number | null` (both null = article not negotiable, full price only) — the OLD design had a single `Merchant.haggle_dc` consulted for every article in the shop and a `discount_percent` re-typed by the GM at every single purchase attempt; now each article carries its own DC and discount, configured once. `Merchant.haggle_dc` itself is kept (unremoved, all existing data/tests stay valid) but demoted to a prefill suggestion for new articles, no longer read by the purchase/haggle routes — `POST .../haggle` and the inline `purchase` `haggle` body both dropped `discount_percent` entirely (always resolved server-side from the article) and both now 400 with `not_negotiable` if the article's `haggle_dc`/`haggle_discount_percent` aren't set. (2) New full-screen `MerchantShopOverlay.tsx`: a tile grid of the shop's articles (card art or a stylized set-code placeholder for boosters, a "Négociable"/"Épuisé" badge) on the left, a detail panel on the right — for a player, price/stock/negotiation terms + the same roll→reroll→buy `PurchaseWidget` flow from the entry above; for the GM, live-editable price/stock/DC/discount inputs (a "Négociable" checkbox reveals the DC/discount fields, unchecking clears both to null) plus "Retirer l'article", with "+ Ajouter un article" still opening the existing `MerchantItemPickerOverlay` nested inside. `MerchantPanel.tsx` is now just merchant creation/deletion and a compact card-per-merchant picker ("Ouvrir la boutique") — all per-item browsing, purchase, and editing moved into the new overlay, replacing the old cramped inline list entirely rather than living alongside it.
  - **Coin toss / dice roll as a duel card effect** (e.g. `Duel.TossCoin`/`Duel.DiceRoll` in a card's Lua script): confirmed NOT Luck-reroll-able, and confirmed **structurally impossible** without patching ocgcore's own C++ source — not merely unimplemented like the other gaps this session. Initial assumption (wrong, corrected after reading the real engine source rather than guessing from generic OCG protocol knowledge): thought this was an undecoded AWAITING prompt (`MessageType` has no `TOSS_COIN`/`TOSS_DICE` entry) that would soft-lock the duel. Actually verified live against `edo9300/ygopro-core`'s `operations.cpp` (`field::process(Processors::TossCoin&)`/`TossDice&`): the engine resolves the toss with its own internal RNG (`pduel->get_next_integer(0,1)` / `(1,6)`) *before* it ever builds the `MSG_TOSS_COIN`/`MSG_TOSS_DICE` message — the message is a pure post-hoc announcement, never a request the engine pauses on (no `AWAITING`/`RESPOND` step exists for it in the engine's own state machine, unlike every other prompt type this app has integrated). There is no point in the flow where the app could intercept and ask "reroll or keep?" — the randomness is already committed, entirely inside the vendored engine, before the app is even told. Making it Luck-reroll-able would require forking/patching ocgcore's C++ RNG call itself to accept an externally-supplied result instead of generating its own — real surgery on a third-party AGPL C++ dependency (with correctness risk to everything else that shares the same RNG stream, e.g. deck shuffling) that would need to be maintained indefinitely against a freshly re-cloned upstream on every build. User decision after being shown this finding: not worth it — dropped, no further action planned unless a real card in actual play makes it worth revisiting.
- [x] **Import bug report investigated (BLGG import failing with a blank "Une erreur est survenue")** (`CardImportPanel.tsx`) — the backend was verified correct twice (a direct call to the import logic, and a real authenticated HTTP call from inside the backend container — both succeeded instantly, 146 cards imported in under a second); root cause concluded to be a dev-server HMR restart interrupting the in-flight browser fetch (surfaces as a `TypeError`, not an `ApiError`, explaining the generic fallback). Two real, smaller frontend bugs fixed along the way regardless: the generic error fallback only handled `ApiError` (broadened to any `Error`), and `loadCards()` ran inside the same try/catch as the import call itself (a later failure there could be misattributed to the import) — separated.
- [x] **Booster/pack images surfaced in the import list and merchant inventories** (`CardSet.model.ts`, `card.routes.ts`, `CardImportPanel.tsx`, `MerchantItemPickerOverlay.tsx`) — `set_image` (fetched from YGOPRODeck's `cardsets.php`) is now stored per `CardSet` and exposed on `ApiCardSet`, displayed both in the import list and the merchant booster picker.
- [x] **Custom card creation form was missing the mandatory Lua script field entirely — real bug, every custom-card creation was silently failing server-side validation** (`CustomCardPanel.tsx`, `frontend/src/lib/api.ts`) — confirmed the UI never surfaced `lua_script` and `createCustomCard`/`updateCustomCard` never sent it, despite the backend requiring it unconditionally (§3.4, no "vanilla" fallback). Fixed with a `.lua` file-upload + textarea field in `CreateCustomCardForm`, and `luaScript` added as an explicit parameter to both API calls.
- [x] **GM can create an empty custom booster directly (not only as a side effect of linking a card to a new one), and the merchant item picker can actually find custom boosters** (`customCard.routes.ts` `POST /custom-cards/boosters`, `card.routes.ts` `GET /cards/sets`) — real bug reported: `GET /cards/sets` unconditionally excluded `is_custom` sets, so a custom booster — even one already linked to a card — never showed up in `MerchantItemPickerOverlay`, making it impossible to stock. Fixed with an `include_custom` query param (default excludes, since importing doesn't apply to custom sets; `include_custom=true` returns official sets plus only the calling GM's own custom sets, never another GM's). New `CustomBoosterManager`/`CustomBoosterRow` section in `CustomCardPanel.tsx` lets the GM create a booster up front and add/remove existing custom cards to/from it directly, instead of only being able to create one as a side effect of linking a card via the older per-card flow.
- [x] **Forgot-password flow: email + 6-digit code** (`PasswordReset.model.ts`, a new `nodemailer`-based email service, `auth.routes.ts`, `AuthPanel.tsx`/`useAuth.ts`) — `POST /auth/forgot-password` sends a 6-digit code (console-logged as a graceful fallback when SMTP isn't configured); `POST /auth/reset-password` verifies it. Backed by a *persisted* Mongo `PasswordReset` model (hashed code, 15-minute TTL via a native Mongo TTL index, 5-attempt lockout) rather than the in-memory pattern used elsewhere in this app, specifically because `tsx watch` restarts the dev backend on every file save and a real in-flight reset request needs to survive that. Both routes use anti-enumeration generic responses — same message/error regardless of whether the email exists or exactly why a code was rejected. Pinned `nodemailer@^9.0.6` (the initially-chosen `^6.9.16` had multiple real CVEs, confirmed via `npm audit`) plus `@types/nodemailer` (v9 doesn't bundle its own TypeScript declarations).
- [x] **Custom card image editing, plus two real booster-sync bugs fixed** (`customCard.routes.ts`, `CustomCardPanel.tsx`, `frontend/src/lib/api.ts`) — three GM-reported items addressed together. (1) New owner-only `PATCH /custom-cards/:id/image` route lets the GM replace just a custom card's image without resubmitting the entire card plus a fresh Lua script, which the existing full `PATCH /:id` requires — a new `ImageEditForm` in `CustomCardRow` reuses the same upload pattern already used at creation. (2) Linking a card to an *existing* booster from the card-side UI (`BoosterLinkForm`) silently failed to list any booster with zero cards in it yet — its dropdown was derived from `cards.flatMap(c => c.card_sets)` (cards' own set links), which is blind to a booster created empty via `CustomBoosterManager`. (3) Creating a *new* booster from the card-side "link" flow worked server-side but never appeared in `CustomBoosterManager`'s list, since the two components fetched the custom-boosters list independently with nothing keeping them in sync. Root cause for both (2) and (3): two unsynchronized fetches of the same underlying data. Fixed by lifting the boosters list (`api.listCardSets(token, {include_custom:true})` filtered to `is_custom`) up to the shared parent `CustomCardPanel`, passed down to both consumers alongside one shared `loadBoosters` refresh callback triggered after any create/link action from either side. New backend e2e test covers the image route (owner-only; rest of the card unchanged). Full suite verified green (284/284 tests, 28 files) + `tsc --noEmit`/`vite build` clean on both sides.
- [x] **Delete a custom booster** (`customCard.routes.ts` `DELETE /custom-cards/boosters/:setCode`, `CustomCardPanel.tsx`) — owner-only; unlinks (not deletes) any custom card still linked to it (`Card.updateMany($pull card_sets)`) before removing the `CardSet` document itself, mirroring the non-destructive behavior of the existing single-card unlink route. Blocked with `409 booster_in_use` in two cases that would otherwise leave a dangling reference (an `open-booster`/purchase call would then 404 with "Set introuvable"): a merchant still stocking it as an item (`item_type: 'booster'`, matching `set_code`) — the GM must remove that article first; or a character already holding unopened sealed copies of it — those must be opened first. `CustomBoosterRow` gained a "Supprimer"/"Confirmer"/"Annuler" control (click-to-confirm, no native `confirm()`) that calls the parent's `loadBoosters` refresh on success. New backend e2e describe block covers all four paths (success + unlink verified, 403 for a different GM's booster, 404 for an unknown `set_code`, and the two 409 blocks in sequence — merchant item removed then character's sealed copies cleared before deletion finally succeeds). Full suite green (287/287, 28 files) + `tsc --noEmit`/`vite build` clean both sides.
- [x] **Pendulum Summon completely blocked — real bug, root-caused to two independent gaps found by reading the vendored engine source directly** (`DuelBoardOverlay.tsx`, `services/ocgcoreClient.ts`, `services/ygoprodeck.ts`, `services/cardImport.ts`) — user-reported: attempting a Pendulum Summon left them stuck, unable to summon or activate anything.
  - **Root cause (the actual blocker)**: `availablePlaces` (frontend, driving both the click-to-act zone highlighting and the list-based `select_place` prompt panel) and `firstAvailablePlace` (backend, same decode logic, used by e2e test setup) both decoded the Spell/Trap-zone half of `MSG_SELECT_PLACE`'s flag as only 5 bits (`0x1f00`/`bit < 5`) — covering the 5 normal S/T zones but never bits 13-15 (Field Zone + the two Pendulum Zones). Confirmed by reading `operations.cpp` directly (`flag = ((flag & 0xff) << 8) | ...` — the szone portion is a full byte, 8 zones, matching the board's own already-confirmed 8-slot `spell_trap_zones` layout: 0-4 normal, 5 = Field, 6-7 = Pendulum). Consequence: setting a Pendulum monster into a Pendulum Zone had NO eligible destination ever offered, regardless of real availability — a hard dead end with no way to complete the action (Pendulum Summon itself, confirmed by reading `processor.cpp`'s `IdleCommand` step, isn't a distinct engine message at all — it surfaces as a normal `activatable` idle-command entry, already correctly decoded; it's specifically the *zone-placement step beforehand* that was unreachable). Fixed by extending both to the real 8-bit range; new unit test (`ocgcoreClient.test.ts`) locks in a Pendulum-Zone-only-free scenario that would have returned `null` pre-fix. `select_place`'s list panel also gained real zone labels ("Zone Terrain", "Zone Pendule gauche/droite") instead of a generic "Magie/Piège N" for every non-normal S/T slot.
  - **Second, independent bug found while investigating (display-only, but directly adjacent)**: `pendulum_scale`/`link_arrows` were NEVER populated for an official (non-custom) card — `YgoCard` (`ygoprodeck.ts`) didn't even declare YGOPRODeck's `scale`/`linkmarkers` fields, so `importCardsForSet` had nothing to copy from. Confirmed live against the real `cardinfo.php` (Odd-Eyes Pendulum Dragon → `scale: 4`; Decode Talker → `linkmarkers: ["Top","Bottom-Left","Bottom-Right"]`) and against the dev database (338 cards with a `pendulum` `frame_type`, all with `pendulum_scale: null`). Consequence: `DeckEditorOverlay.tsx`'s "Échelle Pendule N" line never rendered for any official Pendulum monster — a real Pendulum deck was unbuildable with any visibility into scale values. Fixed by declaring the fields and mapping them (`link_arrows` lower-cased to match the existing custom-card `LinkArrow` convention). New `cardImport.test.ts` (mocks `fetchCardsBySet`, no network) covers both a Pendulum and a Link card mapping correctly, and a plain card staying null/empty. **Only affects future imports** — already-imported sets keep `pendulum_scale: null` until re-imported (the existing "Réimporter" action in `CardImportPanel.tsx` re-runs `importCardsForSet` and will backfill them); a one-off backfill of all ~499 already-imported official sets was considered but not run without being asked, since it's ~500 real outward-facing calls to YGOPRODeck.
  - Full suite green (290/290, 29 files) + `tsc --noEmit`/`vite build` clean both sides.
- [x] **Real-time invite when the GM creates a duel** (`types/socket.ts` — new `duel_invite` event, `utils/broadcast.ts` `notifyDuelInvite`, `duel.routes.ts`, `App.tsx`, `DuelPanel.tsx`) — user-requested: a player whose character participates in a duel the GM just created should be notified, if they're currently in the session. Distinct from the existing `session_resource_changed('duels')` broadcast (already relayed to `DuelPanel.tsx` for a silent list refetch — that only says "the list changed", not "you're wanted"). `duel_invite` is broadcast to the whole session room (same convention as every other real-time event in this app — no per-user Socket.io room), carrying a `participants` array `{user_id, character_id, character_name, team}`; each connected client filters on its own `user_id` to decide whether to show anything, exactly like `computeCanSeeTeam` already does client-side elsewhere. Built server-side in the same `POST /duels` handler that already builds `plans[team]` (deck/character resolution) — `DeckCardPlan` gained `userId` (the character's owner) purely to target this, never sent to the engine or stored on the duel. NPCs are always excluded (no human user behind them) and the creating GM's own `user_id` is excluded too (deduplicated by `user_id`, so a player controlling 2 participants in the same duel gets exactly one invite). Frontend: `App.tsx` listens for `duel_invite`, keeps a small dismissible list of matched invites, and renders them as a fixed banner ("⚔️ Le MJ vous convoque à un duel : «X» (character)") with "Voir le duel" (opens `DuelBoardOverlay` for that duel via a new controlled `requestedOpenDuelId`/`onRequestedOpenDuelHandled` prop pair on `DuelPanel.tsx`, which still owns its own `openedId` for normal in-list clicks) and a dismiss "✕". New `duelInvite.e2e.test.ts` (real HTTP+Socket.io server, `socket.io-client`, same harness as `realtimeBroadcast.e2e.test.ts`) covers: the invited player receives the right payload while the GM's own socket — which DOES receive the raw broadcast, being in the same room — never has its own `user_id` in `participants`; and a duel with zero human participants (both sides NPCs, pure GM supervision) generates no invite at all. Full suite green (292/292, 30 files) + `tsc --noEmit`/`vite build` clean both sides.
- [x] **GM grants cards to a character's collection — search+quantity, or bulk CSV import** (`character.routes.ts` `POST /:id/collection/add-card` + `/collection/import-csv`, `services/cardImport.ts` `importCardsByIds`, `services/ygoprodeck.ts` `fetchCardsByIds`, `GrantCardsOverlay.tsx`) — completes the §3.5 spec bullet that was documented but never actually implemented (no existing route covered it). Strictly GM-only (`isSessionGm`, not `assertCanManageCharacter` which also allows the owner — a player must never be able to credit themselves cards, same rule already applied to money).
  - **Single add**: `POST /:id/collection/add-card` (`card_id`, `quantity` 1-999) — pushes N copies onto `collection`, returns the resolved card (name/image) for immediate display.
  - **CSV import**: format confirmed to be exactly YGOPRODeck's own "My Collection" export (`cardname,cardq,cardrarity,card_edition,cardset,cardcode,cardid,print_id`, UTF-8 BOM at the head — handled via `csv-parse`'s `bom: true` option) — for migrating a collection already built up in an ongoing session elsewhere (explicit user request, with a real CSV sample provided). Matched by `cardid` (= `ygoprodeck_id`, the official passcode) rather than name/set — far more reliable. Any card missing from the local DB is imported on the fly: new `fetchCardsByIds` (`cardinfo.php?id=a,b,c`, confirmed live that YGOPRODeck accepts a comma-separated id batch and silently omits any unknown id rather than failing the whole batch) and `importCardsByIds`, which never re-requests an id already in the DB (verified: zero network calls for an already-known card). Field mapping (`buildCardUpdateFields`, including the `pendulum_scale`/`link_arrows` fix from the entry above) is now shared between `importCardsForSet` and `importCardsByIds` instead of duplicated. A row with no valid `cardid`/`cardq` is skipped (`skipped`, with the reason) without failing the whole import; an id not found even after a live lookup is reported (`not_found`) rather than silently dropped.
  - `GrantCardsOverlay.tsx` (new, opened from a GM-only "+ Ajouter des cartes (MJ)" button in `CharacterList.tsx`): two tabs, search (clickable card grid reusing the pattern already established in `MerchantItemPickerOverlay.tsx`, quantity, Add button) and CSV import (file upload, detailed summary: cards added/not found/skipped rows).
  - New `collectionImport.e2e.test.ts` (mocks `fetchCardsByIds` the same way `economy.e2e.test.ts` already mocks `fetchCardsBySet`; the real network path was verified separately, live, with real ids from the user's own CSV sample — Big Eye/Castle of Dark Illusions, confirmed fetched and persisted correctly, `pendulum_scale`/`engine_code` included) — covers: single add (GM-only, invalid quantity rejected, unknown card 404), and CSV import end to end (already-known card triggers no network call, missing card fetched live, a row with no `cardid` skipped, an unknown id reported `not_found`, non-GM player rejected).
  - Full suite green (296/296, 31 files) + `tsc --noEmit`/`vite build` clean both sides.
- [x] **One player character per user per session ; the GM can only ever create NPCs** (`character.routes.ts` `POST /characters`, `CharacterSheetForm.tsx`) — user-requested QoL item from friend playtesting feedback, explicitly in preparation for the automatic dice-roll stat modifier (§ next entries below): with more than one player character per account, "the" modifier for a roll would be ambiguous. `POST /characters` now rejects a non-NPC creation from the GM (403) and rejects a second non-NPC creation from the same user in the same session (409 `already_has_character`) — the limit is per user, not global, so different players in the same session are unaffected. Frontend: `is_npc` is no longer a user choice at all (the checkbox is gone) — it's derived entirely from who's submitting (`is_npc: isGm`); the form hides itself behind a short message once a player already has their one character.
  - Nine pre-existing e2e tests across `duel.e2e.test.ts`, `customCard.e2e.test.ts`, `economy.e2e.test.ts` and `sessions.e2e.test.ts` relied on the old permissive behavior (a GM creating a non-NPC "throwaway" character for test setup, or the same shared `player` test user getting a second/third player-character across independent nested scenarios) and broke — each was individually re-audited and fixed on its own merits rather than blanket-patched: GM-created throwaway characters became explicit NPCs where the NPC-ness genuinely didn't matter to what the test verified; scenarios that needed a second *real* player-controlled character (e.g. Duel Tag's 2-duelist camp, previously simulated as one player playing both slots) now register a dedicated fresh player user each — arguably a more realistic scenario anyway (two friends teaming up) than the one it replaced.
  - New `characterCreation.e2e.test.ts` covers the rule directly: GM rejected for a player-character, GM free to create unlimited NPCs, a player's first character succeeds but a second is rejected with the right error code, a player still can't create an NPC (pre-existing rule, checked for regression), and a second distinct player in the same session is unaffected by the first player's limit.
  - Full suite green (301/301, 32 files) + `tsc --noEmit`/`vite build` clean both sides.
- [x] **Booster content preview before buying, with hover-zoom** (`MerchantShopOverlay.tsx` `BoosterContentsPreview`) — user-requested QoL item: a player previously had to buy blind to know what a set could contain. "Voir le contenu possible" toggle, loaded on demand (not on every panel render) via the existing `GET /api/cards?set_code=` catalogue endpoint — no new backend route needed. Uses the full-resolution `image_url` (not `_small`) so the hover-zoom (`hover:scale-[2.5]`) stays crisp, matching the pattern already established in `BoosterOpeningOverlay.tsx`'s `SettledSlot`.
- [x] **Hover-zoom on cards in the import panel** (`CardImportPanel.tsx`) — same QoL request, same pattern: full-res `image_url`, `hover:scale-[2.5]`. Real layout constraint found and fixed while building this: the card grid had `max-h-80 overflow-y-auto` on the SAME element as the cards, which would have silently clipped every hover-zoom (a scrolling container clips at its own bounds, full stop) — split into an outer scrolling wrapper and an inner `[overflow:visible]` grid, the same two-layer structure `BoosterOpeningOverlay.tsx` already uses for exactly this reason. Applied the same fix to `BoosterContentsPreview` above.
- [x] **Dice history collapsed to the last 5 rolls, full history in its own view** (`DicePanel.tsx`) — user-requested QoL item: the classic dice block only shows the 5 most recent rolls now (was: every roll since joining, capped only by a CSS scroll height); a "Voir l'historique complet (N)" link opens a dedicated `DiceHistoryOverlay` modal with the full, unfiltered log. Row rendering extracted into a shared `DiceLogRow` so both views stay in sync.
- [x] **Automatic ability-modifier calculation for dice rolls and merchant haggling** (`sockets/index.ts` `roll_dice`/`reroll_dice`, `merchant.routes.ts` haggle routes, `DicePanel.tsx`, `MerchantShopOverlay.tsx`) — the two biggest user-requested items on this list, done together since the second builds on the first. Formula: +1 per 2 points above 10 in the *effective* stat (with level bonus) — identical to the existing Luck-reroll formula (CLAUDE.md §3.3), computed via a tested-but-previously-unused `abilityModifier()` utility that was apparently already sitting in the codebase in anticipation of this.
  - **Dice panel**: `roll_dice` gains an optional `stat` field (one of the five `StatName`s). Providing it forces a d20 (ability-check convention — any `sides` sent alongside is ignored) and requires `character_id` (rejected otherwise: a stat belongs to a character). The modifier is computed **server-side** from that character's real stats — never trusted from the client, consistent with CLAUDE.md §4. `ActionLogEntry` gained `modifier`/`stat`/`total` (all `null` for a stat-less "classic" roll, unchanged behavior). A reroll reuses the *same* modifier/stat (stored on the in-memory `PendingRoll`, not re-resolved) since the character doesn't change mid-reroll. Frontend: a new stat `<select>` next to the existing character picker (disabled without a character selected), the sides `<select>` replaced by a plain "d20" indicator once a stat is chosen, a live client-side modifier preview (cosmetic only — the server recomputes authoritatively), and the log rows now render "d20+5 → 17 (Charisme)" style output when a modifier is present.
  - **Merchant haggling — real bug fixed along the way, not just a new feature**: the haggle `modifier` was previously a bare client-supplied number (`z.number().int().min(-20).max(30)`, no relation to the character's actual Charisma) accepted at face value by both `POST .../haggle` and the inline `purchase` `haggle` body — a genuine "trust the client" gap the CLAUDE.md guideline explicitly warns against, not merely a missing convenience. Both now compute the modifier server-side via a shared `haggleModifierFor(character)` helper from the character's real Charisma; the request bodies no longer accept a `modifier` field at all (`haggle: {}` — just "yes, I'm negotiating"). Frontend: the manual "Modificateur" number input in `PurchaseWidget` is gone; the resolved server-computed modifier still displays in the roll-result readout as before.
  - Three pre-existing `economy.e2e.test.ts` tests relied on the old client-supplied modifier to force deterministic success/failure outcomes against a real (non-mocked) `rollDie(20)` — each re-derived rather than patched: the "guaranteed success" case turned out to already be guaranteed by the character's real +5 Charisma modifier against that item's DC 5 (min roll 1+5=6 ≥ 5); the "guaranteed failure" cases needed a dedicated item with `haggle_dc: 30` (the schema's max) instead, since even the worst possible roll (1) minus no plausible negative modifier could reach that DC 5 item's failure condition once modifiers stopped being arbitrary — max total 20+5=25 < 30 guarantees failure regardless of the roll, which a DC of 5 never could.
  - New `diceStatModifier.e2e.test.ts` (real HTTP+Socket.io server, same harness as `realtimeBroadcast.e2e.test.ts`) drives the actual `roll_dice`/`reroll_dice` socket flow end to end: a `stat: 'charisma'` roll forces d20 (even when a different `sides` is sent) and returns the correct server-computed modifier/total; a stat-less roll is completely unaffected (`modifier`/`stat`/`total` all `null`); `stat` without `character_id` is rejected; a reroll preserves the original roll's modifier and stat.
  - Full suite green (305/305, 33 files) + `tsc --noEmit`/`vite build` clean both sides.
- [x] **Deck export to a `.ydk` file** (`frontend/src/lib/ydk.ts`, `DeckEditorOverlay.tsx`, `cardDto.ts`) — user-requested: "in case we want to use it on another emulator". `Card.model.ts`'s `engine_code` (already the real engine passcode — identical to `ygoprodeck_id` for an official card, synthetic for a custom one, see CLAUDE.md §5) is now exposed on the shared card DTO (`toCardDto`), so the frontend already has a real passcode for every card in a loaded deck (main/extra, already split server-side) with no new endpoint needed. `buildYdkContent`/`downloadYdk` produce the standard `#main`/`#extra`/`!side` format (one passcode per line, repeated per copy) and trigger a client-side Blob download — `!side` always empty, this app has no side-deck concept. An "Exporter en .ydk" button sits next to the Main/Extra counters in the deckbuilder header. New assertion in `deckbuilding.e2e.test.ts` confirms `ygoprodeck_id` (the fallback passcode source when `engine_code` isn't set, e.g. for a card seeded directly in a test rather than through the real import) round-trips correctly on the deck detail DTO.
- [x] **"Long repos" — GM reloads every character's Luck rerolls to max in one action** (`character.routes.ts` `POST /characters/session/:sessionId/long-rest`, `App.tsx`) — user-requested QoL item: previously the GM could only refill a character's rerolls one at a time (implicitly, via a stats/level edit). New GM-only route loads every character in the session and resets `remaining_luck_rerolls` to `maxLuckRerolls(stats.luck, level)` for each — the exact same formula/helper already used for individual creation and stat/level edits (CLAUDE.md §3.3), just applied session-wide in one call instead of per character. A "🌙 Long repos" button next to the "Personnages du salon" header (GM-only, hidden with zero characters) calls it and refreshes the whole character list from the response. New `longRest.e2e.test.ts`: a non-GM player is rejected (403), an invalid session id is rejected (400), and a real long-rest correctly reloads both an NPC and a player character (rerolls forced to 0/1 beforehand) back to their true max (5, for Chance 20 at level 1) — verified both in the response and directly in the database.
- [x] **Custom card block condensed to name+type, full detail moved to a dedicated overlay; both the custom card and custom booster lists capped with a scrollbar** (`CustomCardPanel.tsx`) — two related user-requested QoL items, done together since the first directly enables the second's row-height math. The embedded "Cartes custom" block no longer renders each card's image/description/card-set chips/action buttons inline — each row is now a single compact `name · type` button. Clicking a row (or the new header-level "Voir toutes les cartes custom" button) opens `CustomCardDetailOverlay`, a full-screen split view reusing the exact same layout idiom as `DeckEditorOverlay.tsx`: a left sidebar lists every custom card (same compact rows, click to switch), the selected card's image and full detail (subtitle, effect text, linked-booster chips with unlink) render large on the left of the main area, and a dedicated actions pane (change image / link to booster / delete — `ImageEditForm`/`BoosterLinkForm` reused as-is, only their trigger location moved) sits on the right, GM-only. Both the compact custom-card list and `CustomBoosterManager`'s booster list are now height-capped (`max-h` + `overflow-y-auto`, tuned to ~10 rows at each list's own row height) instead of growing unbounded — a session with many custom cards/boosters no longer pushes the rest of the page down indefinitely.
- [x] **Stylized full-screen character sheet, always-visible button for a player's own character, and a dedicated large-preview collection view** (`CharacterSheetOverlay.tsx` — new, `CharacterList.tsx`, `character.routes.ts` reused as-is) — two related user-requested items, built together (design direction confirmed with the user beforehand: the collection view lives as a tab inside the sheet rather than a separate standalone overlay, styled as a polished layout in the app's existing accent/arena palette rather than a heavier ornamented theme). A `🧙 <name>` button fixed to the top-right of the viewport (`position: fixed`, so it stays visible regardless of scroll) opens the sheet directly on the viewer's own player character — derived the same way as everywhere else in the app (`characters.find(c => !c.is_npc && c.user_id === currentUserId)`, guaranteed at most one thanks to the one-player-character-per-session rule) — and is absent for the GM (who has no single "own" character) and for a player with none yet. Every character card in the existing compact list also gained a "Voir la fiche" link, so the GM (and anyone, for any character) can open the same sheet.
  - **Real, previously-unbuilt gap closed along the way**: `backstory`/`personality`/`visual_description` were captured at character creation (`CharacterSheetForm.tsx`) but had NO display or edit surface anywhere afterward — confirmed by grep, not assumed. Same for `inventory` (§3.2's "list where you can write what a character has on them" was documented but never actually wired to any UI). The new sheet's "Fiche" tab now displays and, for the owner or GM, edits all four via a new `PATCH`-based `api.updateCharacterProfile` (the existing `PATCH /characters/:id` route already accepted these fields — CLAUDE.md's own money/level/experience/inventory schema already covered them, just never called with them from the frontend).
  - **Fiche tab**: identity header, a styled 5-stat grid showing each raw value alongside its real ability modifier (`abilityModifier(effectiveStat(...))`, the same formula/helper the automatic dice-roll modifier already uses — a nice, accurate touch rather than a cosmetic-only number), an editable RP block (Historique/Personnalité/Description visuelle, single edit-toggle for all three), an editable inventory list (add/remove), and an economy column (money + `MoneyEditor`, GM-only; sealed boosters with the existing open-and-reveal flow; `DeckManager` reused as-is) — all gated by the same `canManage` (owner or GM) convention used everywhere else in the app; a non-owner, non-GM viewer sees a fully read-only sheet.
  - **Collection tab**: the requested "cards displayed large on the left" — reuses the exact split-pane idiom already established in `DeckEditorOverlay.tsx` (fixed-width preview `aside` showing the selected card's full art/stats/text on the left, a filterable/sortable grid — same `cardFilters.ts`/`CollectionFilterModal.tsx` infrastructure — filling the rest of the space) minus the deck-building actions, since this view is for browsing, not constructing a deck.
  - `onCharacterUpdate`'s patch type (`App.tsx`, `CharacterList.tsx`) widened from the original `{money, collection, sealed_boosters, decks}` to also cover `{name, backstory, personality, visual_description, inventory}` — safe by construction (TypeScript's contravariant function-parameter checking already let the same wider callback satisfy every existing narrower consumer, e.g. `MoneyEditor`'s `{money?}`-only prop type, without touching any of them). `MoneyEditor` exported from `CharacterList.tsx` (was module-local) so the new sheet can reuse it verbatim instead of duplicating the credit/set-exact UI.
  - `tsc --noEmit`/`vite build` clean; no backend changes were needed (the profile fields' validation, ownership/GM authorization, and real-time broadcast were already correct and covered by existing tests — only a previously-unused capability of an existing route got a frontend caller).
- [x] **Real, systemic bug fixed: `CardSet.set_code` is NOT actually unique in YGOPRODeck's real data — was silently clobbering sets, confirmed to affect 142 real sets, not just the one reported** (`cardImport.ts` `syncCardSets`) — user-reported: "Legend of Blue Eyes White Dragon (LOB)" wasn't in the available packs, only its 25th Anniversary reprint, and that reprint "only has spells in it". Root-caused by querying the real `cardsets.php` directly rather than guessing: it returns **two genuinely different sets sharing `set_code: "LOB"`** — the real 2002-03-08 set (126 distinct cards) and its 2023 25th Anniversary reprint (14 cards, mostly support) — and confirmed this is systemic, not a one-off: **142 of 644 real distinct set_codes are shared by 2+ real sets** (the dominant pattern being a main set sharing its code with its own Sneak Peek/Special Edition promo), while `(set_code, set_name)` together is genuinely unique across all 1032 real sets. `CardSet.set_code` being `unique: true` in the schema meant `syncCardSets`'s upsert-by-`set_code` let whichever set the API happened to return LAST for a given code silently overwrite the other — for LOB specifically, the 14-card reprint had won, and since `importCardsForSet` fetches by the *locally stored* `set_name`, every "import LOB" click was actually importing the reprint's tiny card pool under the real set's identity.
  - **Fix chosen, scoped deliberately** (two options given to the user, a genuine "how much do you want this to cost" decision — see `git log` for the full comparison): the fast, low-risk fix, not the full schema rework that would let every colliding variant be individually addressable (that would touch `card.routes.ts`, `customCard.routes.ts` boosters, `Merchant.model`/routes, `boosterOpening.ts`, and several frontend components — out of scope here, not requested). `syncCardSets` now deduplicates the sets fetched from the API by `set_code` BEFORE upserting, keeping only the one with the most `num_of_cards` per code — almost always the real main set, since the dominant collision pattern is a main set sharing its code with its own much-smaller promo/Special Edition variant, never the reverse. Known, accepted limitation: a genuinely-wanted promo/Special-Edition variant sharing a code with a bigger set stays inaccessible under this heuristic — real full fix if that's ever needed.
  - New tests in `cardImport.test.ts` (mocks `fetchCardSets`, no network): a two-set collision modeled directly on the real LOB case (reprint received BEFORE the original, to prove the fix doesn't depend on API ordering) keeps only the 355-card original; a set with no colliding code imports normally as a regression check.
  - **Dev database actually fixed live**, not just the code path: re-ran the corrected `syncCardSets` against the real dev Mongo (confirmed `CardSet` for `LOB` flipped from the 14-card reprint to the real 126-card set), then re-ran `importCardsForSet('LOB')` (now resolving the correct set name) to backfill the actual `Card` documents — verified directly: "Blue-Eyes White Dragon" now carries its three real LOB prints (`LOB-001`/`LOB-E001`/`LOB-EN001`) in `card_sets`, and 147 cards are now tagged to the real set name (was 14).
  - Full suite green (310/310, 34 files) + `tsc --noEmit` clean.
- [x] **Follow-up on the set_code collision fix above: a way to actually find and reimport every affected set, not just the reported LOB case** (`CardSet.model.ts`, `cardImport.ts`, `card.routes.ts`, `CardImportPanel.tsx`) — direct user follow-up request: they'd found the bug by opening an already-sold LOB booster in PRODUCTION and getting the wrong cards, and needed a way to fix whichever sets were actually affected there, not just re-derive the one case already fixed in dev by hand. New `CardSet.had_code_collision` (recomputed on every sync, never a stuck one-time flag) is set from `syncCardSets`'s own dedup pass — true whenever a set_code had 2+ real candidates in that sync. `POST /cards/sets/:setCode/import` needed no change at all: it already re-fetches and re-upserts correctly for an already-imported set, it just had no frontend entry point.
  - `CardImportPanel.tsx`: every already-imported set now gets a small "Réimporter" action (previously: a static "Importé" badge with no way to trigger it again at all) — general usefulness beyond this one bug. A set currently flagged `had_code_collision` shows an amber "⚠️ Code partagé" badge instead of the plain green one, and an amber banner appears above the list whenever 1+ *already-imported* sets are flagged (deliberately not the ~140 collided codes that were never imported locally — those can't have wrong data yet, nothing to fix), with a "Réimporter ces N sets" bulk action that reimports them one by one (relies on `ygoprodeck.ts`'s existing shared request throttle rather than adding a second one).
  - **Real, much larger scope discovered while verifying this end-to-end**: re-syncing the actual dev database (not just the test suite) surfaced **119 already-imported sets** carrying this exact risk — not a one-off. All 119 were reimported live against the real YGOPRODeck API (0 failures) as part of verifying the fix actually works at the scale that matters, not just for the single reported case.
  - New tests in `cardImport.test.ts`: a set with a collision gets `had_code_collision: true` and appears in `syncCardSets`'s returned `collidedSetCodes`; a set with no collision doesn't; and — a real edge case worth locking in, not just the happy path — a set that collided on one sync but no longer does on the next (the colliding entry disappeared from the API) gets correctly un-flagged rather than staying stuck `true` forever.
  - Full suite green (311/311, 34 files) + `tsc --noEmit`/`vite build` clean both sides.
- [x] **Merchant shop card hover-zoom moved to a large, centered overlay instead of scaling in place** (`MerchantShopOverlay.tsx` `HoverZoomImage`) — user-requested follow-up: the existing in-place `hover:scale-[2.5]` zoom (`BoosterContentsPreview`'s grid) stayed small/could get clipped near a grid edge. New `HoverZoomImage` wraps a thumbnail `<img>`: on hover it portals a large (`max-h-[85vh] max-w-[85vw]`) copy centered in the viewport over a dimmed backdrop, `pointer-events-none` so it never intercepts the mouse itself — only the underlying thumbnail's own hover/leave drives it. Applied to both the main article tile grid (`ItemTile`, previously no zoom at all on those small 130px thumbnails) and `BoosterContentsPreview`'s grid (replacing the old in-place scale entirely, not layered on top).
- [x] **Follow-up: merchant single-card articles were zoomed from a genuinely low-res source image, not a display-sizing issue; booster tile zoom removed** (`merchant.routes.ts`, `MerchantShopOverlay.tsx`) — user-reported: a card sold directly by a merchant stayed blurry/unreadable even with the new large centered zoom above, unlike cards inside a booster's content preview. Root-caused, not guessed: `POST /:id/items` was snapshotting `card.card_images[0].image_url_small` (a genuine thumbnail) into `MerchantItem.image_url` for a card article — `BoosterContentsPreview`, by contrast, reads the full-resolution `image_url` live from `GET /cards`. No amount of display-side zoom can make a small source image sharp; the earlier hover-zoom feature was correctly *applied*, just fed the wrong image. Fixed at the source: `POST /:id/items` now stores the full-resolution `image_url` for a new card article. New GM-only `POST /:id/refresh-card-images` catches up any article added *before* this fix (re-resolves `image_url` from each item's live `Card` by `card_id`, booster articles untouched since their image comes from `CardSet.set_image`, never a card thumbnail) — surfaced as a "Rafraîchir les images" button in the shop header, shown only when the merchant has at least one card article. Also per direct request: booster tile images no longer get the hover-zoom at all (`ItemTile` now checks `item_type === 'card'` before wrapping in `HoverZoomImage`) — a booster's own cover art is already legible without it.
  - New tests in `economy.e2e.test.ts`: a freshly-added card article's `image_url` is asserted to be the full-res URL (not `_small`); a dedicated scenario simulates a pre-fix article (small URL written directly to Mongo, bypassing the now-fixed route) and confirms `POST .../refresh-card-images` corrects only that article's image, reports the right `updated_count`, and leaves the booster article's image untouched; a non-GM player is rejected (403).
  - **Real dev database backfilled too**, not just the code path: 6 merchants across the dev DB had stale small-resolution card images (found live, not assumed) — all corrected via the same live re-resolution the new route performs, verified directly in Mongo before/after.
  - Full suite green (313/313, 34 files) + `tsc --noEmit`/`vite build` clean both sides.
- [x] **GM must explicitly confirm two previously auto-committing fields: an article's haggle DC/discount, and a character's "fixer le total" money override** (`MerchantShopOverlay.tsx`, `CharacterList.tsx` `MoneyEditor`) — user-requested: both fields previously applied silently on blur (leaving a field with an accidental edit and clicking away would commit it), same underlying pattern in two different places. Both now track the typed value against the last-committed one and only show a "Valider"/"Valider le marchandage" button — and only actually call the API — when it's genuinely different, instead of committing on every blur regardless of intent.
- [x] **Character sheet: sealed boosters now show their cover image, list capped with a scrollbar** (`CharacterSheetOverlay.tsx` `BoostersPanel`) — user-requested. `Character.sealed_boosters` only ever stored `set_code`/`set_name`/`quantity` (no image), so each booster's `set_image` is resolved client-side via a small `GET /cards/sets?search=<set_name>` lookup per distinct booster (matched back by `set_code`, since a search can technically return more than one hit) — no backend/schema change needed. The list is capped at a fixed height (`max-h-72`, ~5 rows) with `overflow-y-auto` instead of growing unbounded.
- [x] **Character sheet: free-text "Notes" field for important in-play information** (`Character.model.ts`, `character.routes.ts`, `CharacterSheetOverlay.tsx` `NotesPanel`) — user-requested, distinct from the existing RP block (backstory/personality/visual description, more fixed at character creation): a `notes` field meant to be edited often during play. New `Character.notes` (default `''`, `maxlength: 5000`) goes through the exact same `PATCH /characters/:id` path and owner-or-GM permission already used for the RP fields (`updateCharacterProfile`) — no new route needed. New assertion in `economy.e2e.test.ts` confirms a player can update their own character's notes. Full suite green (314/314, 34 files) + `tsc --noEmit`/`vite build` clean both sides.
- [x] **Official (non-custom) cards can now be added to a custom booster; fixes a real bug where the "add card" UI could permanently disappear** (`customCard.routes.ts`, `CustomCardPanel.tsx` `CustomBoosterRow`) — two related user reports handled together, the bug turning out to be a direct symptom of the missing feature. Root cause: the only way to add a card to a custom booster was picking from the GM's own custom cards *not yet linked to that booster* (a list derived client-side from the session's custom-card array) — once every custom card in the session was already linked, that list emptied and the entire "add" section vanished with no way to add anything else, custom or not.
  - New booster-centric routes (`POST`/`DELETE /custom-cards/boosters/:setCode/cards`) link/unlink an **existing card by id, custom or official**, authorized by *booster* ownership (`loadOwnedCustomBoosterOrThrow`, reused as-is) rather than *card* ownership — a custom card still requires its own creator to link it (checked explicitly, since a custom card stays under its creator's exclusive control per CLAUDE.md §3.4 even into one of their own boosters), but an official card has no owner at all, so booster ownership alone is sufficient. Returns the general `toCardDto` (from `cardDto.ts`), not the custom-card-shaped `toCustomCardDto`, since the linked card may not be custom. Deliberately additive: the existing card-centric `POST /:id/booster-link` (custom cards only, can create a new booster on the fly) is untouched, still used by the per-card "Lier à un booster" flow in `CustomCardDetailOverlay`.
  - `CustomBoosterRow` reworked to match: booster contents are now resolved live from the full catalogue (`GET /cards?set_code=`, the same query `BoosterContentsPreview` already uses on the merchant side) instead of being derived from the custom-cards-only list passed down from the parent — this is what makes the pool of addable cards structurally unable to run dry again. The old single-select dropdown is replaced by a debounced search box (`GET /cards?search=`, which already returns official and custom cards mixed, no query change needed) — click a result to link it, with an "officielle" tag distinguishing non-custom entries in both the search results and the booster's content list. `CustomBoosterManager`/`CustomBoosterRow` no longer need the `cards`/`onCardUpdated` props at all, now that content isn't derived from that array.
  - New describe block in `customCard.e2e.test.ts` (7 tests): a non-GM and a *different* GM are both rejected (booster-ownership check, not card-ownership); a genuinely official card links and shows up via `GET /cards?set_code=`; a second official card links right after the first (proving the pool doesn't run dry, the exact bug reported); double-linking the same card is rejected (`already_linked`); unlinking works and a not-linked unlink 404s; a *different* GM's own custom card stays protected even for the booster's owner (card-level ownership still enforced for custom cards specifically).
  - Full suite green (322/322, 34 files) + `tsc --noEmit`/`vite build` clean both sides.
- [x] **Custom booster detail overlay: click a booster for a visual, image-based view of its contents and of cards to add, split official/custom** (`CustomCardPanel.tsx` — new `CustomBoosterDetailOverlay`/`BoosterCardGrid`) — direct user follow-up to the previous entry, same "Voir toutes les cartes custom" idiom applied to boosters: the compact `CustomBoosterRow` in the list is now name+delete only (its inline expand/search from the previous entry moved here), clicking it opens a full-screen overlay. Left/main pane shows the booster's real contents as image grids (`BoosterCardGrid`, reused for both sides), split into two titled sections — "Officielles" and "Custom" — each a hover-to-reveal "Retirer" button per card; right pane is the same debounced full-catalogue search from before, now rendered as image grids too and *also* split officielles/custom, with a hover-to-reveal "+ Ajouter" button. Both panes resolve from the same live `GET /cards?set_code=`/`GET /cards?search=` queries as before — no backend change needed, this was purely a frontend presentation upgrade (list → visual split grids in a dedicated overlay) on top of the already-correct data layer from the previous entry.
- [x] **Email verification at registration — but only when SMTP is actually configured** (`PendingRegistration.model.ts` — new, `services/email.ts`, `auth.routes.ts`, `useAuth.ts`, `AuthPanel.tsx`) — user-requested: avoid polluting the account list with unreachable/mistyped emails, without making registration a dead end for a deployment that never configured SMTP. New `isEmailConfigured()` (`services/email.ts`, checks `SMTP_HOST`) is the single gate: `POST /auth/register` keeps its exact historical behavior (immediate `User` creation, token returned) when it's false; when true, no `User` is created yet — a `PendingRegistration` (new model, same design as `PasswordReset.model.ts`: persisted in Mongo rather than in-memory so it survives a dev `tsx watch` restart, hashed code, 15-minute TTL index, 5-attempt lockout, but holding the *entire* pending identity — username, email, already-bcrypt-hashed password — since no `User` exists yet to hang a `user_id` off of) is created instead, and a verification email goes out. New `POST /auth/verify-registration` (email + code) is what actually creates the `User` on success, then signs in exactly like `/login` does. A conflicting-username check against *other* still-pending registrations (not just real `User`s) prevents one pending signup from silently blocking another's later verification with a confusing unique-index failure. Re-registering the same still-pending email replaces its old code (same "one active code at a time" rule as password reset) — this doubles as the resend action, no separate endpoint needed.
  - Frontend: `AuthPanel.tsx` gains a `register-verify` step (visually the same idiom as the existing `forgot-reset` step — code input, "Retour à la connexion", now also "Renvoyer le code" by resubmitting the same still-held username/email/password) — entered automatically when `api.register` comes back `pending: true`; when it comes back `pending: false` (SMTP unset), login proceeds immediately exactly as before, no UI change in that case.
  - `passwordReset.e2e.test.ts`'s email mock had to be updated: it replaces the entire `services/email` module, and `auth.routes.ts` now also imports `isEmailConfigured` from it — without an explicit `isEmailConfigured: () => false` added to that mock, every `/register` call in that file would have hit the new pending-verification branch and broken its assumption of an immediate token back.
  - New `registrationVerification.e2e.test.ts` (9 tests, `isEmailConfigured` mocked `true` to exercise the new path end to end): register doesn't create the `User` yet and sends a code; the right code creates it and logs in directly; a wrong code is rejected without creating the account; 5 failed attempts lock the code out; re-registering the same email supersedes the old code; a username already claimed by another *pending* registration is rejected before any email is even sent; a real existing `User`'s username/email is still rejected immediately (regression check on the unchanged case); verifying an email with no pending registration at all gets a generic 400; the stored code and password are both confirmed hashed, never plaintext.
  - Full suite green (331/331, 36 files) + `tsc --noEmit`/`vite build` clean both sides.
